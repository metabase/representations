name: Schema

on:
  push:
    branches: [main]
  pull_request:

jobs:
  schema-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install

      - run: bun run src/cli.js lint --folder examples/v1
